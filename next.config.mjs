/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'bootdey.com', 'res.cloudinary.com', 'preline.co'],
    }
};

export default nextConfig;
